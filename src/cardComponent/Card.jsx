import React from "react";
import "./card.css";
import { useState, useEffect } from "react";

export const Card = (props) => {
  console.log(props);

  return (
    <div
      // style={{ backgroundImage: `url(${imige})` }}
      className="movie"
    ></div>
  );
};
